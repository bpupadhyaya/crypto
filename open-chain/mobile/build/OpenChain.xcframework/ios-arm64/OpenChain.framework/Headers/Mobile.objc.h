// Objective-C API for talking to openchain/mobile Go package.
//   gobind -lang=objc openchain/mobile
//
// File is generated by gobind. Do not edit.

#ifndef __Mobile_H__
#define __Mobile_H__

@import Foundation;
#include "ref.h"
#include "Universe.objc.h"


@class MobileNodeStatus;

/**
 * NodeStatus describes the current state of the P2P node.
 */
@interface MobileNodeStatus : NSObject <goSeqRefInterface> {
}
@property(strong, readonly) _Nonnull id _ref;

- (nonnull instancetype)initWithRef:(_Nonnull id)ref;
- (nonnull instancetype)init;
@property (nonatomic) NSString* _Nonnull peerID;
@property (nonatomic) long peerCount;
@property (nonatomic) int64_t latestHeight;
@property (nonatomic) BOOL syncing;
@property (nonatomic) NSString* _Nonnull chainID;
@end

/**
 * BroadcastTransaction broadcasts a hex-encoded transaction to the network.
 */
FOUNDATION_EXPORT BOOL MobileBroadcastTransaction(NSString* _Nullable txHex, NSError* _Nullable* _Nullable error);

/**
 * GetBalance returns a JSON balance for the given address from local state.
In the current implementation, this is a placeholder that will be connected
to the Cosmos SDK query layer.
 */
FOUNDATION_EXPORT NSString* _Nonnull MobileGetBalance(NSString* _Nullable address, NSError* _Nullable* _Nullable error);

/**
 * GetBlockHeader returns a JSON-encoded block header at the given height.
 */
FOUNDATION_EXPORT NSString* _Nonnull MobileGetBlockHeader(int64_t height, NSError* _Nullable* _Nullable error);

/**
 * GetLatestHeight returns the latest synced block height.
 */
FOUNDATION_EXPORT int64_t MobileGetLatestHeight(void);

/**
 * GetNodeStatus returns a JSON object describing the node's current state.
 */
FOUNDATION_EXPORT NSString* _Nonnull MobileGetNodeStatus(NSError* _Nullable* _Nullable error);

/**
 * GetPeers returns a JSON array of connected peer information.
 */
FOUNDATION_EXPORT NSString* _Nonnull MobileGetPeers(NSError* _Nullable* _Nullable error);

/**
 * GetValidatorAddress returns the validator's address as a hex string.
 */
FOUNDATION_EXPORT NSString* _Nonnull MobileGetValidatorAddress(NSError* _Nullable* _Nullable error);

/**
 * GetValidatorPubKey returns the validator's public key as a hex string.
 */
FOUNDATION_EXPORT NSString* _Nonnull MobileGetValidatorPubKey(NSError* _Nullable* _Nullable error);

/**
 * InitValidator derives a validator keypair from a mnemonic and chain ID.
In production this would use BIP39 + BIP32 derivation; for now we use
a deterministic seed derived from the mnemonic via SHA-256.
 */
FOUNDATION_EXPORT BOOL MobileInitValidator(NSString* _Nullable mnemonic, NSString* _Nullable chainID, NSError* _Nullable* _Nullable error);

/**
 * IsValidator returns true if this node is configured as a validator.
 */
FOUNDATION_EXPORT BOOL MobileIsValidator(void);

/**
 * SignBlock signs block bytes with the validator's private key.
Returns the Ed25519 signature as a byte slice.
 */
FOUNDATION_EXPORT NSData* _Nullable MobileSignBlock(NSData* _Nullable blockBytes, NSError* _Nullable* _Nullable error);

/**
 * StartNode initializes and starts a P2P node. configJSON should be a
JSON-encoded NodeConfig. Returns the peer ID string on success.
 */
FOUNDATION_EXPORT NSString* _Nonnull MobileStartNode(NSString* _Nullable configJSON, NSError* _Nullable* _Nullable error);

/**
 * StopNode gracefully shuts down the running P2P node.
 */
FOUNDATION_EXPORT BOOL MobileStopNode(NSError* _Nullable* _Nullable error);

/**
 * SubscribeBlocks registers a callback that fires when new blocks arrive.
The callback receives a JSON-encoded block header string.
Note: gomobile does not support func params directly, so this uses a
polling approach via GetLatestHeight instead. This function sets up
internal subscription.
 */
FOUNDATION_EXPORT BOOL MobileSubscribeBlocks(NSError* _Nullable* _Nullable error);

/**
 * VerifyBlockSignature verifies that a block signature is valid for the
given public key and block bytes.
 */
FOUNDATION_EXPORT BOOL MobileVerifyBlockSignature(NSString* _Nullable pubKeyHex, NSData* _Nullable blockBytes, NSData* _Nullable signatureBytes, BOOL* _Nullable ret0_, NSError* _Nullable* _Nullable error);

#endif
